

module.exports = {
  // the root directory for all workplaces. Has to be an absolute path.
  workplaceDirectory: process.env.HOME + "/repos",
  // uses the shell command for the editor, so put that here.
  defaultEditor: "code"
}